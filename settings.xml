<settings>
    <mirrors>
        <mirror>
            <id>nexus</id>
            <name>jenkins</name>
            <url>http://192.168.1.57:8081/repository/maven-proxy/</url>
            <mirrorOf>*</mirrorOf>
        </mirror>
    </mirrors>

    <servers>
        <server>
            <id>nexus</id>
            <username>${env.NEXUS_USER}</username>
            <password>${env.NEXUS_PASS}</password>
        </server>
    </servers>

</settings>
